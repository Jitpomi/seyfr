<resources>
    <style name="Theme.Seyfr" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background to white to match the compose background -->
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <!-- Handoff theme -->
        <item name="postSplashScreenTheme">@style/Theme.Seyfr</item>
    </style>
</resources>
